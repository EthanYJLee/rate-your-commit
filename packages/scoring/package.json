{
  "name": "@rateyourcommit/scoring",
  "version": "0.0.1",
  "private": true,
  "description": "Pure, dependency-free scoring functions. No I/O, no AI — see docs/AI-POLICY.md.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^4.1.0"
  }
}
